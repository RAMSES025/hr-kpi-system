#!/bin/bash
# Двойной клик по этому файлу запускает приложение "Удалёнка"
# через собственное окружение проекта (venv), где установлены
# все нужные библиотеки — а не через обычный Python системы.
cd "$(dirname "$0")"
venv/bin/python3 Desktop.py
